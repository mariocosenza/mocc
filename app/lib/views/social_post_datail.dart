import 'package:flutter/material.dart';

class SocialPostDatail extends StatelessWidget {
  const SocialPostDatail({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}